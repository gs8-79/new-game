# 协作边界与合并评审清单（成员3）

> 依据：项目根目录 `TEAM_ASSIGNMENT.md`（五人分工与协作说明，§8 协作边界）。
> 目的：让其他成员在 merge 之前，一眼看清**哪些文件是我的、哪些需要谁确认、我有没有碰不该碰的东西**。

---

## 1. 我改动的文件（按归属分类）

### 1.1 我负责的模块（成员3：地图任务与小队系统）

| 文件 | 改动 | 说明 |
| --- | --- | --- |
| `formal/src/expansion_game.cpp` | 修改 | 新增路线提示生成；修复 5 处失败路径一致性问题（M3-1…M3-5，见 `04-优化与改动说明.md` §4.3） |
| `formal/include/tribe/expansion_game.hpp` | 修改 | 新增常量与接口（新增，不删不改既有声明） |
| `formal/src/world_map_catalog.cpp` | 修改 | 新增 16 条地点档案表 `kProfiles`、BFS 最短路 `roadPredecessors`、`profile/roadDistance/roadPath/nextStepToward` 实现 |
| `formal/src/world_map_catalog.hpp` | 修改 | 新增 `LocationProfile` 与 4 个只读查询接口的声明 |
| `deliverables/member3-map-mission/**` | 新增 | 本交付物目录（文档、测试、工具、实录） |

### 1.2 属于其他成员、但我需要动的文件（**请对应负责人确认**）

| 文件 | 归属 | 我的改动 | 为什么必须动 | 不确认也能回退吗 |
| --- | --- | --- | --- | --- |
| `formal/src/console_ui.cpp` | 成员4（交互与界面） | **纯新增约 25 行**：地图页节点后的结算点 `★`、图例行加一项、地图页底部两行“路线提示/探索提示”（读取只读函数 `missionRouteHint`） | 5.2 要求“增加地图路线提示，减少玩家迷路”，提示文本只有显示在地图页才对玩家生效 | ✅ 可以：删掉这 3 处即可，**没有任何测试或规则依赖它**（已实测：删除后**全部测试仍通过**） |
| `CMakeLists.txt` | 成员1（集成）/ 成员5（构建） | 测试目标加 1 行源文件 + 1 个 include 路径；新增**可选**目标 `tribe-map-playthrough`（`option(TRIBE_BUILD_DELIVERY_TOOLS)`，**默认 OFF**）；`TRIBE_FORMAT_SOURCES` 加 3 个文件 | 交付测试要参与 `ctest`；试玩工具要能复现 | ✅ 可以：删除测试源那一行只会少跑 13 个交付用例，主程序与既有测试不受影响 |

### 1.3 我**没有**碰的东西（按 `TEAM_ASSIGNMENT.md` §8 第 4 条自查）

| 禁止项（§8-4） | 本次是否触碰 | 证据 |
| --- | --- | --- |
| 公共**结构体**字段 | ❌ 未改动 | `ExpansionState` / `GameState` / `WorldLocationInfo` / `Squad` 等字段一字未动 |
| 公共**枚举** | ❌ 未改动 | `WorldLocationId` / `MissionKind` / `ResourceKind` / `CommandId` 等未增删改 |
| **存档字段** | ❌ 未改动 | `save_codec.cpp` 与 `kSaveVersion = 6` 未改动；新增能力全部是“按需推导的只读数据” |
| **命令名称** | ❌ 未改动 | `game_command_catalog.cpp` 未改动；中英文与数字别名保持原样 |
| 存档模块文件 | ❌ 未改动 | `save_repository.cpp` / `save_file_transaction.*` / `save_codec.*` 未改动 |
| 玩法规则文件 | ❌ 未改动 | `game_engine*.cpp`（除只读文本视图未被改）与 `population_rules` / `war_rules` / `seasonal_event_rules` 未改动 |
| 测试目录 `formal/tests/` | ❌ 未改动 | 交付测试放在我自己的交付目录，避免与成员5冲突 |

### 1.4 需要成员1确认的“公开接口新增”（§8-4 的通知义务）

我新增了 2 个公开声明，属于**接口新增**而非结构变更，但仍按 §8-4 主动报备：

| 新增公开声明 | 位置 | 影响 |
| --- | --- | --- |
| `constexpr int kMaximumHarvestActions / kOutpostWoodCost / kOutpostStoneCost / kRockfangEncounterLife` | `formal/include/tribe/expansion_game.hpp` | 只是把原有字面量命名化，数值与原代码完全一致（4 / 6 / 4 / 14） |
| `std::string missionRouteHint(const ExpansionState&)`、`ExpansionGame::routeHintText()`、`world_map::profile/roadDistance/roadPath/nextStepToward`、`world_map::LocationProfile` | `expansion_game.hpp`、`world_map_catalog.hpp` | 纯新增只读接口，无调用方需要改动；其他模块可选择性使用 |

## 2. 合并评审清单（Reviewer Checklist）

按顺序执行即可完成一次完整评审：

```bash
# 1. 默认构建必须与其他成员完全一致（不含我的可选工具目标）
cmake -S . -B out/review-default -DCMAKE_BUILD_TYPE=Release
cmake --build out/review-default --parallel
./out/review-default/tribe-formal-tests        # 期望：全部通过（团队 main 46/46；本地快照 47/47）

# 2. 一键跑完我的交付验收（含可选工具、试玩实录、Mermaid 图）
bash deliverables/member3-map-mission/tools/run-delivery-checks.sh

# 3. 只读检查：确认我没有改存档/枚举/命令名
grep -n "kSaveVersion" formal/include/tribe/game_state.hpp      # 仍为 6
```

| 评审要点 | 期望结果 | 怎么看 |
| --- | --- | --- |
| 主程序行为不变 | `tribe-dawn` 可正常启动、经营与地图命令照旧 | 手工输错命令应看到“任务已结算/当前没有遭遇战”等精确提示，而不是“无法识别该命令” |
| 既有测试不回归 | 既有回归用例全部通过（`main` 33 项 / 本地 34 项） | `test-run-Release.log` 中 `[FAIL]` 数量为 0 |
| 存档兼容 | 老存档仍可读写 | 跑 `formal/tests/save_repository_tests.cpp` 的 v5→v6 迁移用例；本次未改任何持久化字段 |
| 地图正确 | 16 地点 / 17 条道路、双向、连通 | `docs/02` 第 1 节表格 + `artifacts/map-diagrams.md` 的 Mermaid 图（图由代码生成） |
| 结算规则 | 只有 `settle` 能入库 | 交付测试「结算只能在营地或已建前哨完成…」「未结算的载货不进入部落仓库…」 |
| 失败路径原子性 | 被拒绝的命令不改任何字段 | 交付测试逐字段快照比较（19 个字段） |
| 交付路线可复现 | 45 条命令 0 失败 | `bash deliverables/member3-map-mission/tools/run-delivery-checks.sh` 末尾应打印“失败数：0” |

## 2.5 给成员1课程报告的数据（交付物统计，供引用/更新）

根目录 `课程设计报告.md` 已经引用了成员3的实现与交付物（§4.7 图 4-7 BFS 流程图、§5.2 `std::reverse` 用例、
§6.x 地图元数据适配器、附录中的测试与交付物清单）。代码实现与报告描述一致（`previous[from] = from`、
`vector` 当队列、并列取小编号、回溯后 `std::reverse`），**无需修改报告正文**；
但下表统计数字在交付收尾时又有变化，请按最新值更新：

| 报告中的说法 | 交付收尾后的实际值 |
| --- | --- |
| `deliverables/member3-map-mission/tests/map_mission_delivery_tests.cpp` **930 行**、13 个用例 189 条断言 | ✅ 未变：**930 行**、13 用例、189 断言 |
| `deliverables/member3-map-mission/docs/` **1,585 行**、"6 份说明文档" | ⚠️ 已变化：交付收尾新增 `docs/06-协作边界与评审清单.md`，`docs/` 现为 **6 份 / 1,691 行**；若把目录根部的 `README.md`(109 行) 与 `UPLOAD-TO-GITHUB.md`(162 行) 一并计入，说明文档共 **8 份 / 1,962 行** |
| `deliverables/member3-map-mission/tools/`（未给行数） | 新增后为 **740 行**（试玩工具 393、路线表 89、一键验收 67、暂存脚本 117、打包脚本 74） |
| `deliverables/member3-map-mission/artifacts/`（未给行数） | **684 行**（试玩实录 381、Mermaid 图 160、测试日志 95、路线命令表 48） |
| 交付目录规模 | **19 个文件 / 4,371 行 / 约 305 KB** |

> 统计口径：`wc -l` 实测（2026-09-15 交付收尾），命令为
> `cd deliverables/member3-map-mission && wc -l $(find . -type f)`。
> 报告中标为“写作期间”的行号可能已漂移，我已在 `docs/01` 附录里补了函数名锚点，便于按名检索。

## 2.6 报告作者可直接复用的素材

| 报告章节需要什么 | 用我的哪份材料 |
| --- | --- |
| 类图/数据结构（地图部分） | `docs/02` 第 1 节要点表 + `docs/03` 第 8 节（`ExpansionState` 字段速查） |
| 流程图：最短路径 BFS | 报告 §4.7 已覆盖；实现与注释见 `formal/src/world_map_catalog.cpp`（`roadPredecessors` / `roadPath`），验证见 `docs/05` 用例 2 |
| 用例图/测试覆盖 | `docs/05` 第 3 节覆盖矩阵（需求→用例→断言） |
| 迭代记录（优化前后对比） | `docs/04` 全篇（含 M3-1…M3-5 问题与修复、影响面与回滚） |
| 演示用图 | `docs/02` 第 1.1 / 5.5 节与 `artifacts/map-diagrams.md` 的 Mermaid 图（可直接粘贴渲染） |
| 试玩录像对应的文字证据 | `artifacts/playthrough-transcript.txt`（45 条命令逐条回执 + 地图页渲染 + 最终摘要） |

## 3. 与队友交付物的对接点

| 队友交付物 | 我的交付物如何配合 |
| --- | --- |
| 成员1：`DESIGN.md` 架构章节、类图、时序图 | `docs/01-项目分析.md` §3（四层架构与依赖方向）、§5（状态与存档）、§7（地图子系统的数据流）可直接引用；`docs/02` §1.1 / §5.5 的 Mermaid 图可作为类图/流程图的补充 |
| 成员5：`TEST_REPORT.md`、Release 试玩包 | `docs/05-测试说明与验收矩阵.md` 的表格可直接并入 TEST_REPORT（含 13 个用例、覆盖矩阵、未覆盖范围声明）；`artifacts/test-run-Release.log` 与 `playthrough-transcript.txt` 是现成的测试记录与试玩证据 |
| 成员4：界面截图与操作速查表 | 地图页新增内容（结算点 `★`、路线提示/探索提示两行）需要更新截图；`artifacts/playthrough-transcript.txt` 里有该页面的真实文本渲染结果 |
| 成员2：玩法规则表 | `docs/03-地图任务说明表.md` 与玩法规则表格式对齐（输入 / 前置条件 / 状态变化 / 失败提示），可直接合并成同一张总表 |

## 4. 已知的合并冲突风险

| 位置 | 风险 | 建议处理 |
| --- | --- | --- |
| `CMakeLists.txt` | 多人都会加测试源或目标，容易冲突 | 我的改动集中在 4 个小块（测试源 1 行、include 1 处、可选目标 1 块、格式化清单 3 行），冲突时按块取舍即可 |
| `formal/src/console_ui.cpp` | 成员4 也在改界面 | 若成员4 已经重构地图页，请把我的 3 处新增按新结构重新插入；必要时直接放弃这部分（功能仍由 `look` 提示覆盖） |
| `deliverables/` 目录 | 其他成员可能也在建同名交付目录 | 我的目录名固定为 `deliverables/member3-map-mission/`，与任何人都不重名 |
| `out/` 构建产物 | 无 `.gitignore`，容易把二进制提交上去 | 见 `UPLOAD-TO-GITHUB.md` 第 4 节的排除清单与建议的 `.gitignore`（`.gitignore.suggested`） |

## 5. 回滚方式（三级，逐级更彻底）

| 级别 | 操作 | 影响 |
| --- | --- | --- |
| 只撤界面 | 删除 `console_ui.cpp` 中新增的 3 处（`★` 星标、图例一项、两行提示） | 地图页回到原版；游戏规则与全部测试均不受影响 |
| 撤交付物 | 删除 `deliverables/member3-map-mission/` 并从 `CMakeLists.txt` 移除 1 行测试源 | 少跑 13 项交付用例；主程序不受影响 |
| 全撤 | 上述两项 + 还原 `expansion_game.*` 与 `world_map_catalog.*` 四个文件 | 地图回到无路线提示状态；**存档仍然兼容**（本次未改任何持久化字段） |
